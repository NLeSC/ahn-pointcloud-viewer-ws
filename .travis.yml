language: java
jdk:
  - oraclejdk8
env:
  - TERM=dumb
install:
  - ./gradlew assemble
script:
  - ./gradlew check
after_success:
  - ./gradlew jacocoTestReport coveralls
cache:
  directories:
    - $HOME/.gradle
